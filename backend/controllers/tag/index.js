const createTag = require('./createTag');
const getAllTags = require('./getAllTags');
const getSelectableTags = require('./getSelectableTags');

module.exports = {
  createTag,
  getAllTags,
  getSelectableTags,
};
